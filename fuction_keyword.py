# 关键字参数赋值
def func(a, b=5, c=10):
    print('a is', a, 'and b is', b, 'and c is', c)

func(3, 7)
# 按照顺序赋值给参数表的前两个变量
func(25, c=24)
# 首先根据顺序把值赋给第一个变量，然后根据关键字把值赋给某个变量
func(c=50, a=100)
# 不考虑参数顺序，只依靠关键字给变量赋值
